boot:
  namespace:
  clusterName:
  bootGitURL: https://github.com/jenkins-x/jenkins-x-boot-helmfile-config.git
  #bootGitURL: https://github.com/rawlingsj/jenkins-x-boot-helmfile-config.git
  bootGitRef: master
  environmentGitOwner:
  userEmail:
  projectID:
  provider: gke
  tls:
    production: no
  zone:
  versionsGitURL: https://github.com/jenkins-x/jenkins-x-versions.git
  versionsGitRef: master

secrets:
  gsm:
    enabled: false
  env:
    enabled: false
    adminUser:
      username:
      password:
    hmacToken:
    pipelineUser:
      username:
      email:
      token: